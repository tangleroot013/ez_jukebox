# Crostini focus-switch resilience: ~85 ms quantum at 48 kHz.
# Increase to 8192 only if 4096 still underruns during window switches.
context.properties = {
    default.clock.min-quantum = 4096
    default.clock.quantum = 4096
}
